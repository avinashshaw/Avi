{
  "name": "shopify-reviews-app",
  "version": "1.0.0",
  "description": "Shopify product reviews app — import reviews from a CSV file (rating, handle, author, email, title, content, images, created_at, country_code).",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "multer": "^1.4.5-lts.1"
  }
}
