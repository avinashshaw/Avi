{
  "name": "shopify-reviews-app",
  "version": "2.0.0",
  "description": "Installable Shopify app — import product reviews from a CSV file (rating, handle, author, email, title, content, images, created_at, country_code) and store them as metaobjects.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@shopify/shopify-api": "^11.0.0",
    "@shopify/shopify-app-express": "^7.0.0",
    "dotenv": "^16.4.5",
    "express": "^5.0.0",
    "multer": "^2.2.0",
    "serve-static": "^1.16.2"
  }
}
